#include "main.h"

/**
 * _abs -gets absolute value
 * @int: input value
 *
 * Description: changes a number to its abolute value
 * Return: int
 */

int _abs(int)
{
	if (int >= 0)
	{
		return (int);
	}
	else
	{
		int + (-2 * int);
		return (int);
	}
}
